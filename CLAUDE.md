## Supabase — regra rígida

- Projeto único deste repo: `vnrfzgbqiagxidcaeanr`.
- Use SOMENTE as tools `mcp__supabase-isabela__*`.
- PROIBIDO usar `mcp__claude_ai_Supabase__*` (conector de conta) ou qualquer MCP Supabase sem `project_ref` na URL: eles enxergam todos os meus projetos e podem escrever no banco errado.
- Nunca chame `list_projects`, `create_project` ou `pause_project`.
- Antes de qualquer `apply_migration` ou `execute_sql` de escrita, confirme o ref (`get_project_url`) e me mostre.
- Migrations ficam em supabase/migrations/ e sobem com `supabase db push`.
